import 'package:flutter/cupertino.dart';

import '../widgets/bottom_nav_bar.dart';

final Map<String, WidgetBuilder> routes = {
  BottomNavBarWidget.routeNav: (context) => const BottomNavBarWidget(),
};
